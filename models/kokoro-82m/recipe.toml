# Recipe for kokoro-82m — the configuration that produced each published bundle.
# Schema and the rules for `status`: ../README.md.
# Run it with:  python3 ../../conversion/zoo_convert.py run <name>

["kokoro-82m"]
script = "export_kokoro.py"
args = []
card = "README.md"
hf_repo = "mlboydaisuke/Kokoro-82M-CoreAI"
source_hf_id = "hexgrad/Kokoro-82M"
bundle = "kokoro_predictor.aimodel"
status = "verified"
notes = [
  "One run emits all three published graphs (predictor / prosody / vocoder). The exporter's own",
  "usage line is `export_kokoro.py --out-dir exports  # export the 3 bundles` — bare, so the",
  "bucket sizes are the defaults; the card shows --token-bucket / --frame-bucket as options.",
]

# ===========================================================================
# Other modalities
# ===========================================================================
